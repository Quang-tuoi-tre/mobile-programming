// Định nghĩa hàm calculateSum
int calculateSum(int a, int b) {
  return a + b;
}

void main() {
  // Gọi hàm calculateSum với hai tham số
  int so1 = 7;
  int so2 = 5;
  int tong = calculateSum(so1, so2);

  // In kết quả ra màn hình
  print("Tổng của $so1 và $so2 là: $tong");
}
